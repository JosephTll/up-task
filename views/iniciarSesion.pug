doctype html
html(lang="en")
    head
        include ./partials/header
    body.login 
        .contenedor-formulario 
            h1 Iniciar Sesion en UpTask 

            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria] 
                            .alerta(class=`${categoria}`)
                                p #{error}

            form#formulario.caja.caja-login(action="/iniciar-sesion" method="POST") 
                .campo 
                    label(for="email") E-mail: 
                    input(type="email" name="email" placeholder="Tu email" value= email) 
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" placeholder="Tu contraseña"  value= password)
                .campo.enviar 
                    input.boton(type="submit" value="Iniciar Sesion") 
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aun no tienes cuenta? Crea una gratis 
                    a(href="/reestablecer") ¿Olvidaste tu contraseña?