doctype html
html(lang="en")
    head
        include ./partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina 
            p Escribe tu nueva contraseña

            form.caja-login(method="POST")
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" placeholder="Tu nuevo password" value= email) 
                .campo.enviar 
                    input.boton(type="submit" value="Reestablecer contraseña") 